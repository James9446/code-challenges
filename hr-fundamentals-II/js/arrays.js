// Write a function rest that returns all the elements in the array except for the first one. HINT: Read about the slice method on MDN and/or experiment with slice at the console like so:

// var numbers = [3, 2, 7, 5];



// console.log(numbers.slice(0),
// numbers.slice(1),
// numbers.slice(2),
// numbers.slice(0, 2));

// Write a function butlast that returns all of the elements in the array except for the last one 
// (you may want to use slice for this one as well).

